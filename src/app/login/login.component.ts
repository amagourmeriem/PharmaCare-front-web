import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  templateUrl: './login.component.html', // Utilisation du fichier HTML
  styleUrls: ['./login.component.css'], // Utilisation du fichier CSS
  standalone: true, // Indique que le composant est standalone
})
export class LoginComponent {}
